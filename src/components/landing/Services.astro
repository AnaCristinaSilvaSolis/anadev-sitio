---
import services from '../../config/services.json';
---

<section id='services' class='py-20 bg-background'>
  <div class='container mx-auto px-6'>
    <h2 class='text-center font-bold text-5xl mb-4 text-foreground'>
      Nuestros Servicios
    </h2>
    <p
      class='text-center text-muted-foreground text-lg mb-16 max-w-2xl mx-auto'
    >
      Ofrecemos soluciones integrales de desarrollo que se adaptan a las
      necesidades específicas de tu negocio.
    </p>

    <div class='grid md:grid-cols-3 gap-8 max-w-6xl mx-auto'>
      {
        services.map((service) => (
          <div class='bg-card p-8 rounded-2xl border border-border transform transition-all duration-300 hover:scale-105 hover:shadow-xl hover:border-accent/50'>
            <h3 class='text-2xl font-bold mb-4 text-foreground'>
              {service.name}
            </h3>
            <p class='text-muted-foreground leading-relaxed'>
              {service.description}
            </p>
          </div>
        ))
      }
    </div>
  </div>
</section>
